

class Dates {
    getDays(hotelId, roomId, since, to) {
        return new Promise((resolve, reject) => {
            return resolve(true);
        })
    }



    getDayArray(since, to) {
        let startDay = since[0] + since[1]
    }

}

module.exports = new Dates();